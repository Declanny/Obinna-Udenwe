[project]
name = "obinna-udenwe-admin"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "pydantic>=2.11",
    "pydantic-settings>=2.8",
    "python-jose[cryptography]",
    "bcrypt",
    "python-multipart",
    "cloudinary",
    "aiosmtplib",
]
